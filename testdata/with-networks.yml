name: mystack
services:
  app:
    image: myapp:latest
    networks:
      - frontend
      - backend
  db:
    image: postgres:16
    networks:
      - backend
networks:
  frontend:
    external: true
  backend:
    external: true
  internal:
    driver: bridge
